import Resource from "./Resource";
import {
  actions as ResourceActions,
  reducer as ResourceReducer,
  initialState as ResourceInitialState,
  Sagas as ResourceSaga,
} from "./redux";

export default Resource;
export { ResourceActions, ResourceReducer, ResourceInitialState, ResourceSaga };
